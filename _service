<services>
  <service name="obs_scm" mode="manual">
    <param name="url">https://github.com/zephyrproject-rtos/crosstool-ng.git</param>
    <param name="scm">git</param>
    <param name="revision">sdk-0.12.0</param>
    <param name="version">sdk-0.12.0</param>
    <param name="versionformat">@PARENT_TAG@-@TAG_OFFSET@</param>
    <param name="versionrewrite-pattern">sdk-(.*)</param>
    <param name="versionrewrite-replacement">\1</param>
    <param name="filename">zephyr-crosstool-ng</param>
  </service>
  <service name="tar" mode="buildtime">
    <param name="exclude">debian</param>
  </service>
  <service name="recompress" mode="buildtime">
    <param name="file">*.tar</param>
    <param name="compression">gz</param>
  </service>
  <service name="set_version" mode="manual" />
</services>